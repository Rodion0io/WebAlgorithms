const height = 5;
const width = 5;

function creatMaze(height, width){
    let matrix = [];
    for (i = 0; i < height; i++){
        arr[i] = [];
        for (j = 0; j < width; j++){
            arr[i][j] = 0;
        }
    }
    let x = Math.floor(Math.random() * (width / 2 - 0)) * 2 + 1;
    let y = Math.floor(Math.random() * (height / 2 - 0)) * 2 + 1;
    matrix[width][height] = 1;

    let array = [];

}